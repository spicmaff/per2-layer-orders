# Paper

`PER2_CANONICAL_MANUSCRIPT.pdf` is the canonical reader-facing manuscript for this public research artifact. Editable source is under `paper/source/` and originates from the canonical submission package.

See the original build instructions in the source package or the repository release-validation documentation before publishing regenerated PDFs, because PDF bytes may contain toolchain-sensitive metadata even when rendered content is unchanged.
