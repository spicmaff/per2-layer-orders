# Reproduction wrappers

This directory contains thin orchestration wrappers over the single active `src/per2/` implementation.

```bash
python reproduction/validate_finite.py
python reproduction/build_reference.py
```

These scripts are not alternate theorem/semantic implementations.
